{
  "name": "tunay-wasi-landing",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:clientes": "VITE_APP_TARGET=clientes vite build --outDir dist/clientes",
    "build:caficultores": "VITE_APP_TARGET=caficultores vite build --outDir dist/caficultores",
    "build:negocios": "VITE_APP_TARGET=mayoristas vite build --outDir dist/negocios",
    "build:all": "tsc --noEmit && npm run build:clientes && npm run build:caficultores && npm run build:negocios",
    "preview": "vite preview",
    "seed": "tsx scripts/seed.ts",
    "seed:force": "tsx scripts/seed.ts --force"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "@tanstack/react-query": "^5.80.5",
    "firebase": "^12.8.0",
    "qrcode.react": "^4.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.24.0",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
